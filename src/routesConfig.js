// src/routesConfig.js
import { Layout } from './layout/Layout.jsx';

const routesConfig = [
    {
      path: "/",
      exact: true,
      component: Layout
    }
  ];
  
  export default routesConfig;